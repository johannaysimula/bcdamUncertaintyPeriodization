/** @jsx jsx */
import { Fragment, memo } from 'react';
import { css, jsx } from '@emotion/react';
import { PrimaryButtonSkeleton } from '../PrimaryButton/skeleton';
const primaryButtonSkeletonStyles = css({
  marginRight: "var(--ds-space-150, 12px)",
  marginLeft: "var(--ds-space-150, 12px)"
});

// Internal only
// eslint-disable-next-line @repo/internal/react/require-jsdoc
export const PrimaryItemsContainerSkeleton = /*#__PURE__*/memo(({
  count
}) => jsx(Fragment, null, Array.from({
  length: count
}, (_, index) => jsx(PrimaryButtonSkeleton, {
  key: index,
  css: primaryButtonSkeletonStyles
}))));