/** @jsx jsx */
import { css, jsx } from '@emotion/react';
const badgeContainerStyles = css({
  position: 'absolute',
  top: `${"var(--ds-space-negative-050, -4px)"}`,
  right: `${"var(--ds-space-negative-025, -2px)"}`,
  pointerEvents: 'none'
});
const containerStyles = css({
  position: 'relative'
});

// Not exported to consumers
// eslint-disable-next-line @repo/internal/react/require-jsdoc
export const BadgeContainer = props => {
  const {
    id: badgeId,
    badge: Badge,
    children
  } = props;
  return jsx("div", {
    css: containerStyles
  }, children, jsx("div", {
    css: badgeContainerStyles,
    id: badgeId,
    "aria-hidden": true
  }, jsx(Badge, null)));
};