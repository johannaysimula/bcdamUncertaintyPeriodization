import { ThemeProps, ThemeTokens } from '@atlaskit/button/types';
import { NavigationTheme } from '../../theme';
export declare const getPrimaryButtonTheme: ({ mode: { primaryButton } }: NavigationTheme) => (current: (props: ThemeProps) => ThemeTokens, props: ThemeProps) => ThemeTokens;
