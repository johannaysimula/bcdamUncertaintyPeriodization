import { ThemeProps, ThemeTokens } from '@atlaskit/button/types';
import { NavigationTheme } from '../../theme';
export declare const getIconButtonTheme: ({ mode: { iconButton } }: NavigationTheme) => (current: (props: ThemeProps) => ThemeTokens, props: ThemeProps) => ThemeTokens;
