export class InProductTestPageObject {
  constructor(cy) {
    this.cy = cy;
  }
  toTestId(id) {
    return `[data-testid="${id}"]`;
  }
  toAriaLabel(label) {
    return `[aria-label="${label}"]`;
  }
}