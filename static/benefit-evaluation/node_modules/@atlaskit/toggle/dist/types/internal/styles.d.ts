import { SerializedStyles } from '@emotion/react';
import { ThemeModes } from '@atlaskit/theme/types';
import { Size } from '../types';
export declare const getStyles: (size: Size, mode: ThemeModes) => SerializedStyles;
