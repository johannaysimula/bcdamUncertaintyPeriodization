import { createTheme } from '../utils/create-theme';

// Create default global light theme
var dest = createTheme(function () {
  return {
    mode: 'light'
  };
});
var Provider = dest.Provider;
var Consumer = dest.Consumer;
var useTheme = dest.useTheme;
export { useTheme as useGlobalTheme };
var Theme = {
  Provider: Provider,
  Consumer: Consumer
};
// eslint-disable-next-line @repo/internal/react/require-jsdoc
export default Theme;